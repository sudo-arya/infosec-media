import React from "react";
import Select from "../Components/Select";

const Rental = () => {
   const arg = "Rent";

  return (
    <div>
      {/* rent */}
      <Select arg={arg} />
    </div>
  );
};

export default Rental;
